% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_dwca_metadata.R
\name{extract_dwca_metadata}
\alias{extract_dwca_metadata}
\title{Anhadir metadatos EML y numero de occurrences a una tabla con columna dwca_url}
\usage{
extract_dwca_metadata(df, progress = TRUE)
}
\arguments{
\item{df}{\code{data.frame} que debe contener una columna llamada \code{dwca_url}.
Puede incluir columnas adicionales, como por ejemplo \code{recurso_fk} o
\code{url_ipt}, que se conservarán en la salida.}

\item{progress}{Si \code{TRUE}, muestra progreso por consola.}
}
\value{
El mismo \code{df} de entrada, conservando sus columnas originales y anhadiendo:
\itemize{
\item \code{eml_title}
\item \code{eml_version}
\item \code{eml_pub_date}
\item \code{eml_occurrences}
\item \code{eml_status}
\item \code{eml_error_message}
}

Si una URL no puede procesarse correctamente, las columnas extraidas quedarán
como \code{NA} y el detalle del problema se registrará en \code{eml_error_message}.
}
\description{
Procesa una tabla que debe contener una columna llamada \code{dwca_url} con URLs a
archivos ZIP de tipo Darwin Core Archive (DwC-A). Para cada URL:
\itemize{
\item descarga el ZIP,
\item lee \code{eml.xml},
\item extrae \code{title}
\item extrae \code{packageId} y parsea solo la version (por ejemplo \code{V2.2}),
\item extrae \code{pubDate},
\item lee \code{meta.xml},
\item localiza el fichero de occurrences a partir de \code{rowType} y \code{location},
\item cuenta sus registros restando \code{ignoreHeaderLines}.
}

Para mejorar la eficiencia, cada URL unica se procesa una sola vez aunque
aparezca repetida en varias filas.
}
\details{
Esta funcion asume que:
\itemize{
\item la URL del recurso DwC-A está en la columna \code{dwca_url}
\item el titulo está en un nodo \code{title} de \code{eml.xml}
\item la version está en el atributo \code{packageId} del nodo raiz de \code{eml.xml}
\item la fecha está en el nodo \code{pubDate}
\item el fichero de occurrences está definido en \code{meta.xml}
\item las lineas de cabecera se indican en el atributo \code{ignoreHeaderLines}
}

El conteo de occurrences se realiza en streaming, sin cargar el fichero
completo en memoria.
}
\examples{
\dontrun{
out <- extract_dwca_metadata(df)
}

}
